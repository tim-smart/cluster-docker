apiVersion: v1
kind: Service
metadata:
  name: shard-manager
  namespace: shard-manager
spec:
  selector:
    app: shard-manager
  ports:
    - name: tcp
      targetPort: 8080
      port: 8080
  clusterIP: None
